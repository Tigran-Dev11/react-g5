import CountCompany from "../../components/countComponent"
import * as S from "./styled"

export const CountPage=()=>{
    return (
        <S.Count>
            <CountCompany/>
        </S.Count>
    )
}